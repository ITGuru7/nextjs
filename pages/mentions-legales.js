import Typography from "@material-ui/core/Typography";

export default () => {
  return (
    <div style={{height: '100vh'}}>
      <Typography
        variant="headline"
        color="primary"
      >
        {`Mentions Légales`}
      </Typography>
    </div>
  );
};
